# Backend
PORT=3001
PGHOST=localhost
PGPORT=5434
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=justarter
PGPOOL_MAX=10
PG_IDLE_TIMEOUT_MS=30000
PG_CONNECTION_TIMEOUT_MS=5000
# Porta publicada pelo serviço PostgreSQL no host.
POSTGRES_PORT=5434
# DATABASE_URL=postgresql://postgres:postgres@localhost:5434/justarter

# GraphQL gateway
GATEWAY_PORT=4000
BACKEND_URL=http://localhost:3001

# Frontend
VITE_GRAPHQL_URL=http://localhost:4000/
